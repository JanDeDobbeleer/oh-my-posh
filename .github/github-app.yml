scripts:
- name: Install Dependencies
  command: |
    apm install
    git restore apm.lock.yaml
    pip install ast-grep-cli
  triggers:
  - session.create
automation:
  auto_issue_session: true
  remote_control: true
