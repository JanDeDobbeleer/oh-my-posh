scripts:
- name: Install Dependencies
  command: |
    apm install --frozen
    git restore apm.lock.yaml
    pip install ast-grep-cli
  triggers:
  - session.create
automation:
  auto_issue_session: true
  remote_control: true
