a couple of pointers to fix:

* For the preview element, just remove the preview outer container. It's okay if we only show the SVG so that it's cleaner on the page.
* We can remove the preview text because we no longer need it.
* For config, also remove the config text. Move the JSON, YAML, and TOML selectors to the left, so left-align those. The JSON selector somehow is not horizontally aligned with YAML and TOML. That needs to be fixed.
* The config container should wrap and only have one trailing space always (on start). It needs to be adjusted in height based on the text that's in the container.

---

I did not mean line wrapping. The line wrapping doesn't need to happen. What I meant is the height of the config container that contains the configuration text. If you look at segments, there's a lot of white space below the last bracket, but there should always only be one trailing space: one white line, one blank line. Below the last character in the text, currently it's 4 to 5, which seems to be hard-coded in the initial height.