In the `themes-svg` branch, commit 5cf09a5f ("refactor(website): give the sidebar a lighter caret and no collapse") accidentally committed two stray scratch files that don't belong in the repo: `website/_scratch_schema.json` and `website/_scratch_templates.txt`. They look like leftover debug output from an unrelated prior task (a dumped segment-properties schema and a template listing), not part of the sidebar-caret change the commit is about.

Please remove them with a small cleanup commit: `git rm website/_scratch_schema.json website/_scratch_templates.txt` and commit as something like "chore(website): remove stray scratch files committed by mistake". Verify `npm run build` (or whatever the website's normal build/lint check is) still passes afterward, and confirm no code references these files (they shouldn't, given the names, but check `grep -r _scratch_schema` and `grep -r _scratch_templates` across the repo first to be safe).