Create a plan and implement it with Sonnet 5 as a subagent. I'll do the manual validation. Also update the docs.

---

<task-notification>
<task-id>ab2c0ac70ef04a537</task-id>
<tool-use-id>toolu_01S63zdomQ8NUgK5oXX5dNX1</tool-use-id>
<output-file>C:\Users\jande\AppData\Local\Temp\claude\D--oh-my-posh\69d8706c-d098-4239-8a99-f89c153822c3\tasks\ab2c0ac70ef04a537.output</output-file>
<status>completed</status>
<summary>Agent "Implement yash shell support" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>No CR characters anywhere in the file. All verification complete.

## Report

**Files created:**
- `D:\oh-my-posh\src\shell\scripts\omp.yash` — yash init script (LF endings, no BOM, verified byte-for-byte)
- `D:\oh-my-posh\src\shell\yash.go` — embeds the script, implements `Features.Yash()`
- `D:\oh-my-posh\src\shell\yash_test.go` — tests RPrompt/FTCSMarks/Upgrade/Notice output and asserts all unsupported features return empty
- `D:\oh-my-posh\website\static\skills\ohmyposh\shell\yash.md` — yash setup skill doc

**Files modified:**
- `D:\oh-my-posh\src\shell\constants.go` — added `YASH = "yash"`
- `D:\oh-my-posh\src\shell\init.go` — added YASH to the `Init()` shell-support switch, `generateScript()` (QuotePosixStr + yashInit), `sourceCommand()` (dot-source case, separate from ZSH/BASH since yash has no `source` builtin), `sessionScript()`
- `D:\oh-my-posh\src\shell\features.go` — wired `feature.Yash()` into `Lines()`
- `D:\oh-my-posh\src\shell\formats.go` — `GetFormats`: `case BASH:` → `case BASH, YASH:`
- `D:\oh-my-posh\src\terminal\writer.go` — `FormatTitle`: added `shell.YASH` alongside BASH/ZSH
- `D:\oh-my-posh\src\shell\dsc.go` — added `.yashrc` path resolution and `eval "$(...)"` command wrapping for YASH
- `D:\oh-my-posh\src\cli\init.go` — added `"yash"` to `supportedShells` and the `Use:` string
- `D:\oh-my-posh\website\docs\installation\prompt.mdx` — added yash tab (between xonsh/zsh)
- `D:\oh-my-posh\website\docs\installation\customize.mdx` — added yash tab
- `D:\oh-my-posh\website\docs\dsc.md` — added `**yash**: Configures .yashrc` bullet
- `D:\oh-my-posh\website\static\skills\ohmyposh\SKILL.md` — added Yash link

`packages\msix\dsc\oh-my-posh.shell.dsc.resource.json` was checked but contains no shell-name enum, so no change was needed there.

**Test/build results:**
- `gofmt -l .` (whole src tree): no output — clean
- `go build ./...`: succeeds
- `go test ./shell/... ./terminal/... ./prompt/... ./cli/...`: all pass, including new `TestYashFeatures` and `TestYashFeaturesUnsupported`

**Sanity checks:**
1. `go run . init yash --print --config ..\themes\jandedobbeleer.omp.json` prints `export POSH_SESSION_ID=...; export POSH_CONFIG=$'...';` followed by the full omp.yash body (exports, `_omp_preexec`/`_omp_hook`, `YASH_PS1`/`YASH_PS2`, and — since RPrompt was enabled by the theme's features — the appended `YASH_PS1R` block). Running without `--print` (i.e. plain `init yash`) confirms the dot-source line: `... . $'/c/Users/.../init.&lt;hash&gt;.yash'`, using `.` rather than `source`, as required.
2. `go run . print primary --shell=yash --terminal-width=120` renders a prompt whose byte dump shows `\[ \x1b[38;2;...m \]` wrapper sequences around every ANSI escape — confirming `GetFormats` correctly reuses the BASH `\[ \]` non-printing markers for yash.

**Deviations:** None from the plan. Vale (via the `vale-user-facing-text` skill) was run against all touched/new docs; the only findings were pre-existing em-dash style in the shell-doc title template (`# Oh My Posh — &lt;Shell&gt; Setup`), which matches every sibling file (`bash.md`, `xonsh.md`, etc.) exactly, so it was left as-is to stay consistent with the existing convention rather than to fix a newly introduced issue.</result>
<usage><subagent_tokens>110287</subagent_tokens><tool_uses>74</tool_uses><duration_ms>334757</duration_ms></usage>
</task-notification>