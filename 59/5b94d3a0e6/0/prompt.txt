I get .: cannot open file `$/home/jan/.cache/oh-my-posh/init.1322256362671581267.yash': No such file or directory

---

Jobs seem to work at least increasing, but when they finish, it seems that the counter is not decremented, so it only increments. That works perfectly, but the decrement on job finishing doesn't seem to work. The known literal to eyeball the slash also works. That's not an issue. Everything else just seems to work, so that's good.