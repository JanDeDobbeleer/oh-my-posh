https://github.com/JanDeDobbeleer/oh-my-posh/issues/7718 this issue reports that the `oh-my-posh` string now effectively breaks, which is true because we changed something to strip ANSI escape sequences. I want to keep that documentation, which still contains the explanation to use `oh-my-posh` string, but in reality I want people to move towards using the populated JSON (which we do to populate the normal hit segment using the normal template) and not this ANSI-driven environment variable.

We need to update the docs: either remove that part or adjust it so that it talks about how to enable it. Maybe it's already there. I did not check, so validate that, and then we can close this issue with that guideline. Maybe also make an FAQ item on "Hey, this does not render anymore. How do I then migrate from the `oh-my-posh` string environment variable to enabling `oh-my-posh` and leveraging this correctly?"