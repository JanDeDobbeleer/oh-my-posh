why are you still using vale?

---

yes

---

how does rendered, err := template.Render(color.String(), nil) solve the user's request when no context is passed?

---

Why aren't we using table-driven tests for these test cases?