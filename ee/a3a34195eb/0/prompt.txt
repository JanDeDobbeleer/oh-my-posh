In D:\oh-my-posh\src\segments\plastic.go, Plastic.Init (line ~39) overrides Base.Init but only sets p.options and p.env — it never sets p.Segment = &Segment{} (see Base.Init in src/segments/base.go). Base.SetText/Text/SetIndex dereference b.Segment, so a plastic segment that becomes enabled (cm command installed) panics with a nil pointer dereference when the prompt renders and calls SetText. Fix Plastic.Init to call p.Base.Init(props, env) (or set Segment explicitly) while keeping its extra initialization, verify no other segment shadows Init the same way (grep for "func (.*) Init(props options.Provider, env runtime.Environment)" under src/segments/), and add a regression test that calls Init then SetText on the Plastic writer. Module root is src/.