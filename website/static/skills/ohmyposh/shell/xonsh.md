# Oh My Posh — Xonsh Setup

## Configure

Add this as the **last line** of `~/.xonshrc`:

```bash
execx($(oh-my-posh init xonsh))
```

Reload:

```bash
exec xonsh
```

## Next step

→ [Customize your prompt](/skills/ohmyposh/configuration.md)
